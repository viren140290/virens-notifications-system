task default: %w[test]

task :test do
  puts "hello"
end

task :traffic_from_home_to_work do
  ruby "tasks/traffic_from_home_to_work.rb"
end

task :traffic_from_work_to_home do
  ruby "tasks/traffic_from_work_to_home.rb"
end

task :dollar_to_rupee do
  ruby "tasks/dollar_to_rupee.rb"
end

task :weather do
  ruby "tasks/weather.rb"
end

task :reminder_at_4 do
  ruby "tasks/reminder_at_4.rb"
end

task :test do
  ruby "tasks/reminder_at_4.rb"
end

task :vaccine_info do
  ruby "tasks/vaccine_info.rb"
end
